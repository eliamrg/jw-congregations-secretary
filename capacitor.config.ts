import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: "JW Congregation's Secretary",
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
